// // Solution to excercise 1
// SCRIPTS.filter(v => v.name[0] === 'A' && v.direction === 'rtl')
//     .map(m => {
//         return {
//             name: m.name,
//         };
//     });


// // Solution to excercise 2
// SCRIPTS.filter(f => f.living)
//     .map(m => m.year)
//     .reduce((acc, value) => value > acc ? value : acc);
